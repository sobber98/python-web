services:
  python-manager:
    build:
      context: .
    image: python-management-platform:latest
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./config.toml:/app/config.toml:ro
      - python-manager-data:/app/data

volumes:
  python-manager-data:
